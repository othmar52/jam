/* session{session.paddedCounter} begin */
window.tracklist.push({
  sessionIndex : 'session{session.paddedCounter}',
  sessionDir: '{session.dirName}',
  tracks: [
{trackItems}
  ]
});
window.hostLevel = '{hostLevel}';
/* session{session.paddedCounter} end */
